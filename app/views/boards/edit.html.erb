<div style="max-width: 600px; margin: 60px auto; padding: 0 20px; font-family: 'Helvetica Neue', Arial, sans-serif;">
  <div style="text-align: center; margin-bottom: 40px;">
    <h1 style="font-size: 20px; font-weight: 300; letter-spacing: 0.15em; margin-bottom: 10px; text-transform: uppercase;">Edit Post</h1>
    <div style="font-size: 10px; color: #999; letter-spacing: 0.2em;">掲示板 編集</div>
  </div>

<%# app/views/boards/_form.html.erbのコードをedit.html.erbと共通で使い回す %>
  <%= form_with(model: @board, local: true) do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</div>